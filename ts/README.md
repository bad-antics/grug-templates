# my-project

Simple TypeScript project. No complexity.

## Run

```bash
# Dev (ts-node)
npm run dev

# Build + run
npm run build && npm start
```

## Philosophy

- Minimal dependencies
- Strict TypeScript
- Simple > clever
